<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or "My KDP Books" }}</title>

    <!-- Bootstrap CSS (CDN option shown) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet" />

    <!-- Your custom CSS -->
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <!-- Hero -->
    <section
      class="hero text-white"
      style="
        background-image: url('/images/book-cover.jpg');
        background-size: cover;
        background-position: center;
      ">
      <div
        style="
          position: absolute;
          inset: 0;
          background: rgba(0, 0, 0, 0.45);
          z-index: 0;
        "></div>
      <script>
        (function () {
          var parent =
            document.currentScript && document.currentScript.parentElement;
          if (!parent) return;
          if (getComputedStyle(parent).position === "static")
            parent.style.position = "relative";
          var c = parent.querySelector(".container");
          if (c) c.style.zIndex = 1;
        })();
      </script>

      <div class="container py-5 position-relative">
        <div class="row align-items-center">
          <!-- Text column: on small screens it appears after the image, on md+ it appears first (left) -->
          <div
            class="col-12 col-md-7 order-1 order-md-1 text-md-start text-center">
            <h1 class="display-5 fw-bold mb-3">WELCOME</h1>
            <h1 class="display-5 fw-bold mb-3">to Dounya's book store</h1>

            <p class="lead mb-4">
              A collection of my published books on Kindle Direct Publishing
              (KDP).
            </p>
            <a
              href="#books_collection"
              class="btn btn-lg"
              style="
                background: #f898a4;
                color: #fff;
                border: none;
                box-shadow: none;
                border-radius: 8px;
                padding: 0.6rem 1.2rem;
                text-decoration: none;
                font-weight: 600;
                display: inline-block;
              "
              >Browse Books</a
            >
          </div>

          <!-- Image column: on small screens it appears first, on md+ it appears second (right) -->
          <div
            class="col-12 col-md-5 order-2 order-md-2 text-center mt-4 mt-lg-0">
            <img
              src="/images/book-cover.jpg"
              alt="Book cover"
              class="img-fluid rounded shadow"
              style="max-height: 320px; width: auto" />
          </div>
        </div>
      </div>
    </section>

    <main class="container mt-5" id="books_collection">
      {{ content | safe }}
    </main>

    <!-- Bootstrap JS bundle (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
